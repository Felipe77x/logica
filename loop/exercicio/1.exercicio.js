const readline = require('readline-sync')
do {
    let nota = readline.questionFloat("Digite uma nota:")
} while(nota < 0 || nata > 10)
    soma += nota
    media = soma / 2
    console.log(`A média é: ${nota}`)